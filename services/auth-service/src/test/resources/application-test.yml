spring:
  datasource:
    url: jdbc:h2:mem:auth;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
auth:
  jwt:
    issuer: test-issuer
    audience: test-audience
  cookie:
    secure: false
    same-site: Lax
  allowed-origins: http://localhost:3000
